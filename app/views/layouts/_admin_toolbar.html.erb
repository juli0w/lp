<% [notice, alert].each do |f| %>
    <% unless f.blank? %>
      <div class="information-box round flash"><%= notice || alert %></div>
    <% end %>
  <% end %>

  <!-- TOP BAR -->
  <div id="top-bar">
    
    <div class="page-full-width cf">

      <ul id="nav" class="fl">
  
        <li class="v-sep"><a href="/" class="round button dark ic-left-arrow image-left">Voltar para a Loja</a></li>
        <li class="v-sep"><a href="#" class="round button dark menu-user image-left">Logado como <strong><%= current_user.profile.try(:name) || current_user.email %></strong></a>
          <ul>
            <li><%= link_to "Minhas informações",  dashboard_profile_path %></li>
            <li><%= link_to "Sair",  destroy_user_session_path, :method => :delete %></li>
          </ul> 
        </li>
      
        <li><%= link_to new_messages, messages_path, class: "round button dark menu-email-special image-left" %></li>
        <li><%= link_to "Sair",  destroy_user_session_path, :method => :delete, class: "round button dark menu-logoff image-left" %></li>
        
      </ul> <!-- end nav -->

    </div> <!-- end full-width -->  
  
  </div> <!-- end top-bar -->