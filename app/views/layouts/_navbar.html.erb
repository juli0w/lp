<div class="band main-menu">
  <div class="container">
    <div class="sixteen columns">
      <nav class="navbar">
        <ul class="nav primary_nav">
          <% @categories_root.each do |category_root| %>
            <li><%= link_to category_root.name, category_root %>

              <% if category_root.children.any? %>
              <ul class="sub_menu">

                <% category_root.children.each do |category| %>
                  <li><%= link_to category.name, category %>
                    <ul>
                      <% category.children.each do |subcategory| %>
                        <li><%= link_to subcategory.name, subcategory %></li>
                      <% end %>
                    </ul>
                  </li>
                <% end %>
              </ul>
              <% end %>

            </li>

          <% end %>
        </ul>

        <div class="minicart">
          <a href="#" class="minicart_link" >
            <span class="item"><b>2</b> itens -</span> <span><b>R$ 69,20</b></span>
          </a>
          <div class="cart_drop"> <span class="darw"></span>
            <ul>
              <li>
                <img src="ag" width=46 height=46><a href="#">Clogs Beach/Garden Clog</a> <span class="price">R$ 49,90</span>
              </li>
              <li>
                <img src="ag" width=46 height=46><a href="#">Faded chambray shorts</a> <span class="price">R$ 12,90</span>
              </li>
              <div class="cart_bottom">
                <div class="subtotal_menu"><small>Subtotal:</small>R$ 69,20</div>
                <a href="leisure_cart.html">Checkout</a>
              </div>
            </ul>
          </div>
        </div>
      </nav>
    </div>
  </div>
</div>